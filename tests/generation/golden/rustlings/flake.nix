# Generated by autonix
#
# This flake uses a multi-file structure:
#   .autonix/devShell.nix              - Development shell
#   .autonix/{language}/packages.nix   - Language toolchains and tools
#   .autonix/{language}/*-checks.nix   - Build and test checks
#
{
  description = "Generated by autonix (devShells.default + checks)";

  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
    rust-overlay = {
      url = "github:oxalica/rust-overlay";
      inputs.nixpkgs.follows = "nixpkgs";
    };
  };

  outputs = { self, nixpkgs, flake-utils, rust-overlay }:
    flake-utils.lib.eachDefaultSystem (system:
      let
        overlays = [ (import .autonix/rust/overlay.nix { inherit rust-overlay; }) ];
        pkgs = import nixpkgs { inherit system overlays; };
        lib = pkgs.lib;

        rustPackages = import .autonix/rust/packages.nix { inherit pkgs lib; };

        devPackages = []
          ++ rustPackages.packages;

        notices = []
          ++ rustPackages.notices;

        projectRoot = ./.;

      in
      {
        devShells.default = import .autonix/devShell.nix {
          inherit pkgs lib devPackages notices;
          rustToolchain = rustPackages.rustToolchain or null;
        };

        checks = {}
          // (import .autonix/rust/test-checks.nix { inherit pkgs lib devPackages projectRoot; })
          // (import .autonix/rust/build-checks.nix { inherit pkgs lib devPackages projectRoot; })
          ;
      });
}
